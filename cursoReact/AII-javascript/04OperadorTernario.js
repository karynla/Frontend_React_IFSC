const temperatura = 18;
const clima = temperatura > 20 ? 'quente' : 'frio';
console.log(clima);

const nota = 85;
const conceito = 
nota >= 90 ? "A" :
 nota >= 80 ? "B" : 
 nota >= 70 ? "C" :
  "D";

console.log(conceito);